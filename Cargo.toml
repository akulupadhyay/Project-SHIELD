[workspace]
members = ["src-tauri"]
default-members = ["src-tauri"]
resolver = "2"

[profile.release]
codegen-units = 1
debug = false
incremental = false
lto = "fat"
opt-level = 3
panic = "abort"
strip = "symbols"
